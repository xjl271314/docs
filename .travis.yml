language: node_js
node_js:
  - lts/*
install:
  - npm ci # npm ci
script:
  - npm run build # npm run build
deploy:
  provider: pages
  skip_cleanup: true
  local_dir: docs/.vuepress/dist
  github_token: 08bf5617e0e362ee2f053dce00a12cd201124970
  keep_history: true
  on:
    branch: master